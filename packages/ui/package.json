{
  "name": "@b3pay/ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "B3Pay design system — tokens, brand assets and component primitives.",
  "license": "MIT",
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": "./index.ts",
    "./styles.css": "./styles.css",
    "./assets/*": "./assets/*"
  },
  "files": [
    "index.ts",
    "styles.css",
    "tokens",
    "components",
    "assets",
    "readme.md"
  ],
  "peerDependencies": {
    "react": "^18",
    "lucide-react": ">=0.469.0 <1",
    "sonner": "^2"
  },
  "peerDependenciesComment": "lucide-react publishes as 0.<n>.0 with n in the hundreds, so a caret range like ^0.4 resolves to >=0.4.0 <0.5.0 and matches nothing. 0.469.0 is the version readme.md pins for the CDN build, and the last line with the brand glyphs (Github and friends) still in the set.",
  "dependencies": {
    "@fontsource-variable/jetbrains-mono": "^5.3.0",
    "@fontsource/instrument-serif": "^5.3.0",
    "@fontsource/inter": "^5.3.0",
    "@fontsource/space-grotesk": "^5.3.0"
  }
}
