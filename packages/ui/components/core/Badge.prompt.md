Use `Badge` for status, chain names, workflow visibility and release stage. It renders uppercase mono — pass short labels.

```jsx
<Badge color="success" dot>Live</Badge>
<Badge color="warning" variant="outlined">Beta</Badge>
<Badge color="primary" variant="filled" size="md">ckBTC</Badge>
```

Do not put sentences in a badge. Two words maximum.
