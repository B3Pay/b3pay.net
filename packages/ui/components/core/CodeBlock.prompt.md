```jsx
<CodeBlock filename="src/reactor.ts" code={"export const { useActorQuery } = defineReactor({ name: 'backend' })"} />
<CodeBlock lang="bash" code="pnpm add @ic-reactor/react" />
```

No syntax highlighting by design — the mono face and the filename bar carry it. Keep samples short enough to read without scrolling.
