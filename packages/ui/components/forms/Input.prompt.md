```jsx
<Input size="md" placeholder="Search workflows" icon={Search} />
<Input mono placeholder="ryjl3-tyaaa-aaaaa-aaaba-cai" />
<Input invalid value="bad" onChange={fn} />
```

Set `mono` for any value the Internet Computer defines — principals, canister IDs, Candid expressions, cycles.
