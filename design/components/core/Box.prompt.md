Layout glue. Reach for `Box` instead of a bare `<div style>` so spacing stays on the token scale.

```jsx
<Box direction="row" gap="sm" align="center" padding="md" bg="card" border radius="lg">…</Box>
```
