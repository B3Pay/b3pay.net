```jsx
<Dialog title="Fork this workflow?" description="You get a private copy. The original is untouched."
  onClose={close} footer={<><Button onClick={close}>Cancel</Button><Button variant="filled" color="primary">Fork</Button></>} />
```
